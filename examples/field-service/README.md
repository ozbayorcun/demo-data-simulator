# Field Service Fixture

Tiny app-shaped fixture for testing `demo-data-simulator infer`.

The domain has customers, technicians, work orders, and lifecycle events:

- work order created
- work order scheduled
- work order completed

The `dashboard/` folder shows the generated `events.jsonl` and `metrics_daily.csv`
as a local static operations dashboard. It is synthetic proof data only.
